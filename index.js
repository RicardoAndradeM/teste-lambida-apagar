exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('ovo funcionar'),
    };
    console.log("oporra, deu certa!");
    return response;
};
